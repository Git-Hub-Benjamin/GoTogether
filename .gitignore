# ===============================================
# Gitignore for "GO-TOGETHER" Fullstack Project
# ===============================================

# -----------------------------------------------
# 1. Project-specific Ignored Files
# -----------------------------------------------

# Environment variables (sensitive information)
.env

# Node.js specific ignores for both client and server
client/node_modules/
server/node_modules/

# Common Node.js lock files (if you prefer not to commit them)
# If you use package-lock.json or yarn.lock for exact dependency trees,
# you might want to remove these lines.
client/package-lock.json
server/package-lock.json

# JavaScript/TypeScript build output
# Assuming client/src might compile to a 'dist' or 'build' folder
client/build/
client/dist/

# Assuming server/src might have compiled JS or other build artifacts
server/build/
server/dist/

# Python specific ignores (for your stock analyzer project)
# Assuming your Python files might live at the root or in a 'scripts' folder
*.pyc
__pycache__/
**/venv/
/venv